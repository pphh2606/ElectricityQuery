# 更改日志

## Bug 修复
- 修复电费查询返回后旧结果残留的问题：从余额结果页返回楼栋/房间列表时同步清空已选房间和余额数据，避免下次进入仍显示上一次查询结果（selectedRoom、balance 是 ViewModel 中保存当前查询状态的字段）。
- 修复大厅本地 JSON 兜底数据误导用户的问题：加载内置 JSON 快照时统一清空点赞/收藏信息，下拉刷新时已有服务端数据则不再被本地旧数据覆盖，刷新失败保留当前数据、成功后替换为最新服务端数据（本地快照指 App 内置 JSON，服务端实时数据指从 ehall 接口拉取的数据）。

## 架构改进
- 重构电费查询页返回逻辑：移除余额页“重新选择房间”按钮、房间网格页的悬浮“返回楼栋选择”按钮以及相关多语言文案，系统返回键与顶部返回键按“余额结果 → 楼栋/房间列表 → 校区列表/退出”的层级统一处理（SelectionStep 是控制校区、房间网格、结果页等步骤的枚举状态）。
- 大厅页新增数据来源标记：用 HallDataSource 枚举区分服务端实时数据与本地 JSON 兜底数据，点赞时在本地乐观更新点赞数，刷新后再以服务端结果为准（乐观更新指先即时更新界面，再等待网络请求结果校正）。

## 构建变更
- 将应用版本号从 1445 提升到 1451