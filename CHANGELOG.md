# 更新日志

## 新增功能
- 大厅搜索页新增“重新登录”和“重试”入口，登录过期时用户可以直接跳转登录页重新认证，而不是停留在空白错误页。

## Bug 修复
- 修复账单、通知、充值记录等页面使用 `pluralStringResource` 时未把数量作为格式化参数传入，导致本应显示“已加载 30 条”的地方可能出现 `%1$d` 原始占位符的问题。
- 修复挂失、我的信息、二维码、大厅等模块登录过期提示文案重复且行为不一致的问题，统一使用 `login_expired` 并展示重新登录入口。

## 多语言与文案
- 将电费、账单、大厅、通知、登录、设置、反馈、崩溃报告等展示层硬编码中文迁移到 Android 字符串资源，并补齐阿拉伯语、英语、法语、日语、繁体中文共 675 条翻译，App 会跟随系统语言显示对应文案。

## 架构改进
- 新增 `UiMessage` 消息模型，ViewModel 不再直接拼接中文提示，而是返回资源 ID 和占位参数，由界面按当前语言解析，方便多语言切换和统一错误展示。
- 账单状态、订单状态、支付渠道、语言、转场动画、顶栏样式等枚举和数据模型改为返回 `StringRes` 资源 ID 而不是显示文本，业务层只描述状态、界面负责翻译，避免业务逻辑和界面文案耦合。
- 新增 `checkHardcodedStrings` Gradle 防回归任务，构建执行 `check` 时会扫描 `Text()`、`snackbar` 等展示型 API 中的中文字面量，发现硬编码中文就报错。

## 资源整理
- 删除挂失、我的信息、二维码模块各自的“登录已过期”重复资源，统一由 `login_expired` 提供。

## 工程配置
- 应用版本号从 1491 提升到 1499。
