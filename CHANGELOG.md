# 更新日志

## 体验优化
- 登录失败时给出更明确的提示：把“需要验证码、账号密码错误、获取登录参数失败”等 CAS 异常区分开，用户能直接知道失败原因，其中 CAS 是学校的统一身份认证系统。

## Bug 修复
- 修复扫码登录后可能丢失登录态的问题：如果扫码后的用户信息提取失败，也会把临时 Cookie 同步到系统，保证账号仍处于已登录状态。
- 修复缴费服务大厅 Token 获取不稳定：已有身份令牌会直接复用、同一时间只发起一次获取，并兼容已登录时直接跳转 dlyscas 的流程，避免重复请求和失败。
- 修复办事大厅初始化会话时网络异常处理不完整：统一捕获 IOException，无法连接校园网时继续使用本地 JSON 数据，而不是中断流程。
- 修复 WebVPN 自动登录的稳定性问题：手动 CAS 登录请求不会再误触发自动登录，同一账号的并发自动登录合并为一次，避免请求互相干扰或进入重试死循环。

## 架构改进
- 新增统一 CAS 登录流程：账号密码登录和 WebVPN 自动登录共用“获取登录页、解析表单参数、AES 加密、提交登录”的同一套逻辑，并统一登录异常类型，减少两套流程不一致的风险。
- 新增统一 Cookie 解析器：所有“按名称读取 Cookie、解析 name=value 字符串”的逻辑集中到一处，系统 Cookie 和单用户 Cookie 共用同一套解析规则。
- 新增统一重定向链跟踪器：服务授权和 WebVPN 自动登录共用“跟随 HTTP/JS 重定向直到 CAS 登录页”的逻辑，不再各写一套。
- 简化 OkHttp 客户端工厂：把多个创建方法合并成一个 `create()`，用参数控制超时、是否跟随重定向和是否启用 WebVPN，减少重复构建代码。
- 各业务 API 统一网络配置和会话检测：校园卡、电费、二维码、校园空间、缴费大厅等接口统一使用新的客户端创建方式，并直接复用 HTML 登录页检测逻辑。
- 清理 UI 层小重构：账单详情 URL 改为直接调用 API，WebView 地址切换合并为一个入口，并移除各接口中重复的日志调用。

## 工程配置
- Gradle 增加大文件下载超时设置：连接超时 60 秒、读取超时 10 分钟，避免下载 Android 依赖库（AAR 包）时网络波动导致构建失败。
- 应用版本号从 1469 提升到 1475。

## 删除的文件
- 删除 HTTP 诊断日志工具：移除请求重定向链、Cookie 和失败原因的详细日志，相关调用已从各 API 中清理，减少日志噪音和性能开销。
