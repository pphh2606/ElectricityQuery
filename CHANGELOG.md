# 更新日志
## 新增功能
- 深色模式下新增“纯黑背景”开关：开启后背景和卡片表面改用纯黑/近黑色，OLED 屏幕更省电、显示更统一；实现上通过覆盖 Material 3 主题的 `background`、`surface` 等颜色变量完成。
- 登录过期提示覆盖更多功能页：电费查询、账单、卡挂失、缴费大厅、留言等页面检测到 `SessionExpiredException` 会话过期异常时会显示“重新登录”按钮，而不是只停留在普通错误提示或要求下拉重试。
- 大厅搜索新增“暂无搜索结果”空状态：搜索无结果时明确提示而不是显示空白列表，并已同步多语言文案。
- 留言列表加载更多时增加转圈加载动画：底部“加载中”提示旁显示进度指示器。
## Bug 修复
- 修复内置浏览器把断网或 DNS 失败等加载错误误判为“未登录”并跳转本地登录的问题：现在只有最终地址确实停留在 CAS 登录页且主框架没有加载错误时才跳转。
- 修复从本地登录返回后内置浏览器不刷新、返回行为不明确的问题：登录成功会自动回到原网页并刷新当前页，按返回键则同时退出登录页和 WebView；实现上为 WebView 场景新增了独立的 `webview_login` 路由。
## 架构改进
- 统一各页面错误状态和“重新登录/重试”交互：把分散的错误 UI 收敛到 `ReLoginContent` 公共组件，并让电费、卡中心、缴费大厅、留言等 ViewModel 统一识别会话过期异常。
- 重构 WebView 的 CAS 登录检测逻辑：从 `onPageStarted` 改为 `onPageCommitVisible` 检测，并为 Android 5.0 以下系统保留 `onPageFinished` 兜底检查，减少误判和重复跳转。
## 工程配置
- 应用版本号从 1516 提升到 1523。
