# 更改日志

## 新增功能
- 办事大厅新增“搜索”Tab：支持输入关键字搜索，并可分别按“服务角色”“服务类别”筛选，点击结果即可打开应用（搜索通过 serviceCenterData.json 的 searchKey 参数匹配关键字、labels 参数过滤分类）。
- 办事大厅“全部”页和缴费服务大厅首页新增分类索引栏：页面顶部固定显示横向分类标签，点击会平滑滚动到对应区域，滚动时自动高亮当前分类（索引栏用 LazyRow 横向列表和 FilterChip 标签实现，derivedStateOf 根据列表首个可见项计算当前分类）。

## 架构改进
- 办事大厅数据从扁平应用列表改为分类应用列表：服务端接口切换到 getUserCategoryAppList.json，本地 hall_apps.json 同步更新为新的分类响应，并通过 extractCategories 过滤掉“全部”伪分类和空分类后按分类展示；点赞收藏逻辑也改为在对应分类的 appList 中更新，本地兜底加载时仍会清空点赞/收藏信息（扁平列表指所有应用平铺在一个数组里，分类列表指应用先按 pcAppCategory 分组，appList 是每个分类下的应用列表）。
- 新增大厅搜索接口封装 HallSearchApi，并在首次进入“搜索”Tab 时懒加载：选择角色/类别标签或输入关键字后重新请求，下拉刷新时如果搜索页已打开也会同步刷新（懒加载指用到时才请求，避免一进入大厅就多发请求）。
- 主界面顶栏结构重构：去掉全局 Scaffold 顶栏，首页、大厅、我的页面各自管理标题栏和状态栏边距，大厅页移除旧的 HallTopBar（状态栏边距指内容避开系统状态栏区域的 padding）。

## 界面优化
- 首页顶部索引栏和“我的服务”区块的上下间距略微收紧，页面内容更紧凑（通过调整 LazyColumn 和区块的 padding 实现）。

## 内容更新
- 更新内置大厅应用快照 hall_apps.json：从旧版扁平应用列表换成新版按分类组织的响应结构，加载时继续清空快照中的点赞/收藏信息，避免把本地旧数据当作真实数据（本地快照是打包在 App 内的 JSON 数据，服务端可用时会被实时数据替换）。

## 构建变更
- 将应用版本号从 1451 提升到 1459。
