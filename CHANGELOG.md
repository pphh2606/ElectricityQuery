# 修改日志

## 新增功能
- 首页新增横向索引栏：在标题栏下方固定显示「我的服务」和各分类的快捷入口，点击任意入口即可自动平滑滚动到对应区块，分类较多时无需手动下拉查找
- 索引栏会随页面滚动自动高亮当前区块：根据列表滚动位置切换选中项，让用户随时知道当前停留在哪个服务区域

## 界面优化
- 调整索引按钮样式：未选中按钮改为浅灰色背景并去掉描边，整体使用更圆润的胶囊形状，选中项保持高亮，视觉上更接近轻量快捷标签
- 优化首页分隔线显示：索引栏底部分隔线仅在页面向下滚动后出现，回到顶部自动隐藏；同时移除「我的服务」区域下方的分隔线，减少顶部区域的视觉分割

## 内容更新
- 更新内置首页应用数据：替换为最新版 home_apps.json，新增教务系统测试入口、迎新服务、移动教务 3 个应用，并刷新 15 个应用的图标地址，首页分类、应用网格和索引栏会直接使用新数据
