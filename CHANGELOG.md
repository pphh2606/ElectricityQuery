# 更新日志

## 新增功能
- 语言选择列表现在同时显示本地语言名和中文名，例如 `English（英语）`，让用户在不熟悉的语言下也能确认选项。
- 新增 Android 5.1（API 22）以下系统的专门浅色、深色启动主题资源，旧设备启动页的背景和系统栏颜色也会跟随当前主题。

## Bug 修复
- 修复语言切换后页面重建导致启动时 Cookie 静默校验重复执行、可能误跳登录页的问题，现在每个进程启动后只校验一次。
- 修复部分桌面启动器拒绝创建快捷方式时仍显示成功的问题，现在会根据 `requestPinShortcut` 的返回值提示不支持或失败。

## 多语言与文案
- 统一并修正电费记录和“减少动画”等设置的多种语言文案，中文、英文、法文、日文、阿拉伯文、繁体中文表述更准确。

## 架构改进
- 开启 R8 Full Mode 并增加 5 轮优化、允许访问权限调整、激进接口合并，同时移除 Compose、OkHttp、Coil、Zxing、Navigation 等库的整包保留规则，让发布包的代码压缩和资源删除更彻底。
- 构建产物改为按语言拆分并只打包项目实际支持的语言资源，应用商店可只向用户下发所需语言，减小下载体积（AAB 按语言拆分包）。

## 工程配置
- 为旧版 Android 增加创建桌面快捷方式所需的 `INSTALL_SHORTCUT` 权限，并把语言切换后的页面刷新改为原地重建，避免重新启动整个任务栈。
- 应用版本号从 1499 提升到 1516。
